---
description: Append each turn to dev-ideas/agent-transcripts/YYYY-MM-DD/NNN_sessionId.jsonl (incremental agent log).
alwaysApply: true
---

# Incremental agent transcript log

Maintain a **project-local mirror** of the agent conversation, **one JSONL line per message**, updated **every turn** before finishing the assistant reply.

User explicitly authorized writes under `dev-ideas/agent-transcripts/` (gitignored).

## Path format

```
dev-ideas/agent-transcripts/YYYY-MM-DD/NNN_<session-id>.jsonl
```

| Part | Rule |
| --- | --- |
| `YYYY-MM-DD` | Calendar date of the **current turn** (authoritative session date from the environment) |
| `NNN` | Sequence for that date: `001`, `002`, … `999` (zero-padded). One file per **session id** per day. |
| `<session-id>` | Cursor agent transcript UUID (e.g. `100f4394-bb18-46b3-8d97-7c1729567414`) |

## Resolve session id (first turn, or if unknown)

Each Cursor agent chat has one UUID. The mirror file must use **that** UUID so
turns from chat A never go into chat B’s log.

### Step 0 — is this a new session?

On the **first turn of this agent chat** (and whenever the session id is
unknown), **verify** whether this is a new session before choosing a UUID.
Use the same criteria as `session-prompt-count-and-handoff.mdc`:

| Situation | Session |
| --- | --- |
| First user message points at / attaches a handoff (`dev-ideas/*-handoff.md`) | **New** |
| New Cursor chat / no remembered `session-id` for this chat | **New** |
| Same chat continuing; you already have this chat’s `session-id` | **Continue** — reuse it |

Do **not** ask the user for the UUID. Do **not** block the product task on
logging identity.

### How to get the UUID

1. Prefer **`transcript_location`** in the system/context block (path contains
   `agent-transcripts/<uuid>/…`). Use that `<uuid>`.
2. Else, on a **new** session: look under
   `~/.cursor/projects/home-bvii-repos-Compartarenta/agent-transcripts/<uuid>/`
   for a Cursor JSONL whose **first user message** matches this chat’s first
   user prompt (verbatim or clear prefix). If found, use that `<uuid>`.
3. Else, on a **new** session with no recoverable Cursor id: **create** a fresh
   UUID (`uuidgen` / equivalent lowercase) and use it for the mirror for the
   rest of this chat.
4. If **continuing** and the id was lost: recover only by matching this chat’s
   known first user prompt (or later unique turns) against Cursor JSONL or
   today’s `dev-ideas/agent-transcripts/` mirrors — never by “newest mtime”
   alone.
5. Do **not** pick a UUID solely by sorting folders by modification time. A
   previous chat may have just received a background write.

**Then write only to:**

`dev-ideas/agent-transcripts/YYYY-MM-DD/NNN_<this-chat-uuid>.jsonl`

If that file does not exist yet for today’s date, allocate a new `NNN` (see
below) and create it. Never append this chat’s lines to a file whose UUID is
not this chat’s.

## Allocate `NNN` (first turn only)

List `dev-ideas/agent-transcripts/YYYY-MM-DD/*.jsonl`:

- If a file matching `*_<session-id>.jsonl` already exists → **reuse it** (append only).
- Else `NNN` = one greater than the highest existing prefix for that date; if the directory is empty, use `001`.

Create the directory if missing: `dev-ideas/agent-transcripts/YYYY-MM-DD/`.

## When to write

**Every turn**, after the substantive work and **before** the final user-visible reply:

1. Append a **user** line for the current user message (verbatim text; strip only system wrappers if duplicated).
2. Append an **assistant** line for the final reply you are about to send (full markdown/text).

Do **not** skip because the turn was “small”, a save-only reply, or a path-only `Garde-moi cette réponse` answer — still append both lines.

## JSONL line shape

Match Cursor agent transcript style (one JSON object per line, no outer array):

```json
{"role":"user","message":{"content":[{"type":"text","text":"<verbatim user message>"}]}}
{"role":"assistant","message":{"content":[{"type":"text","text":"<full assistant reply>"}]}}
```

- Escape JSON properly (prefer a short shell/Python one-liner; do not hand-escape multiline strings).
- Optional: append extra assistant lines for **tool rounds** when useful, with `"type":"tool_use"` blocks mirroring Cursor — not required if only the final assistant text is logged.
- Do **not** log secrets (`.env` values, tokens, private keys). Replace with `[REDACTED]`.

## How to append

**Always** use the repository script — do **not** hand-roll `python3 -c` one-liners or
leave placeholder bodies (`PLACEHOLDER`, `echo "skip"`, `if False`, etc.).

From repo root, **turn mode** (preferred at end of each reply):

```bash
USER_MSG="$(mktemp)" ASSISTANT_MSG="$(mktemp)"
trap 'rm -f "$USER_MSG" "$ASSISTANT_MSG"' EXIT
printf '%s' '<verbatim user message>' >"$USER_MSG"
printf '%s' '<full assistant reply>' >"$ASSISTANT_MSG"
python3 tool/append_agent_transcript.py \
  --session-id '<session-id>' \
  --date 'YYYY-MM-DD' \
  --user-file "$USER_MSG" \
  --assistant-file "$ASSISTANT_MSG"
```

Single-line mode when only one role is needed:

```bash
printf '%s' '<message>' | python3 tool/append_agent_transcript.py \
  --session-id '<session-id>' --date 'YYYY-MM-DD' --role user
```

The script resolves `NNN_<session-id>.jsonl`, creates directories, JSON-escapes
text, and exits non-zero on failure. If it fails, say so once briefly and retry
next turn — never silently skip logging.

Or Read existing file + Write only when the shell is unavailable.

## Backfill on first log in a long session

If this is the **first append** in a session that already has many turns:

1. **Read** `~/.cursor/projects/home-bvii-repos-Compartarenta/agent-transcripts/<session-id>/<session-id>.jsonl` when it exists.
2. **Copy** it to the dev-ideas path (creates a full mirror), then append only **new** turns on subsequent steps.

If the Cursor file is missing, start from the current turn and note in chat only if the user asks.

## Internal state across turns

Remember for the session (do not spam the user):

- `session-id`
- `dev-ideas/.../NNN_<session-id>.jsonl` path

## Failure

If append fails (permissions, invalid JSON), say so **once** briefly; retry next turn. Do not block the user's task on logging alone.

## Relation to other rules

- Complements `conversation-test-context-discipline.mdc` (thread memory) with a **durable file** the user can grep.
- Does not replace reading the live thread or Cursor transcript when debugging.
