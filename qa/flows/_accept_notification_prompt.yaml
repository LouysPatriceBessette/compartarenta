appId: com.compartarenta.compartarenta.dev
---
# In-app notification permission prompt (POST_NOTIFICATIONS granted via adb in coordinator).
- runFlow:
    when:
      visible:
        id: "qa-notification-flow-permission-enable"
    commands:
      - tapOn:
          id: "qa-notification-flow-permission-enable"
