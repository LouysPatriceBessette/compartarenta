appId: com.compartarenta.compartarenta.dev
---
# Probe: keep generate-screen polling alive; do not assert bilateral success here.
- extendedWaitUntil:
    visible:
      id: "qa-contacts-invitation-short-code"
    timeout: 30000
- extendedWaitUntil:
    notVisible:
      id: "qa-contacts-invitation-short-code"
    timeout: 60000
    optional: true
