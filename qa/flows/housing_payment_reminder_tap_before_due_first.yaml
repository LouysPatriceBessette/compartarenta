appId: com.compartarenta.compartarenta.dev
---
# OS shade only (before_due). Text tap allowed for Android system shade only —
# match stable product title, never QA "#N" prefixes (temporary, will be removed).
# Journal asserts: housing_payment_reminder_assert_before_due_first.yaml
- extendedWaitUntil:
    visible: ".*Rappel de paiement.*"
    timeout: 30000
- takeScreenshot: phase1_notification_shade
- tapOn: ".*Rappel de paiement.*"
- takeScreenshot: phase1_after_notification_tap
