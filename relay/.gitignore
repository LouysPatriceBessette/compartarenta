# Build outputs and local-only state.
/relay
/dist/

# Real env files (only the `.env.example` template is checked in).
.env
.env.local
.env.*.local

# Editor / OS noise.
.DS_Store
*.swp
*.tmp

# Test artifacts.
*.test
*.out
coverage.out
coverage.html
