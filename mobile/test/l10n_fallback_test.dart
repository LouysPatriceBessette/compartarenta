import 'package:flutter_test/flutter_test.dart';

void main() {
  test('l10n fallback is handled by Flutter gen_l10n', () {
    // This is a placeholder smoke test. The effective fallback behavior is
    // defined by Flutter's localization generation and the presence of keys in
    // the template (English) ARB file.
    expect(true, isTrue);
  });
}

